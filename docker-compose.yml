version: "3"
services:
  frontend:
    build:
      context: /next-js
      dockerfile: /Dockerfile
    ports:
      - "3000:3000" 

  backend:
    build:
      context: /backend/stapi_plain
      dockerfile: /Dockerfile
    ports:
      - "1337:1337" 
